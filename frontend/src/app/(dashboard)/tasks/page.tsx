import { TasksScreen } from '@/custom/screens/tasks/TasksScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function TasksPage() {
  return <TasksScreen />
}
