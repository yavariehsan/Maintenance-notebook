import { SettingsScreen } from '@/custom/screens/settings/SettingsScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function SettingsPage() {
  return <SettingsScreen />
}
