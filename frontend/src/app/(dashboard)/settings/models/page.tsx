import { ModelsScreen } from '@/custom/screens/models/ModelsScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function ApiKeysPage() {
  return <ModelsScreen />
}
