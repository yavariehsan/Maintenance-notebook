import { PodcastsScreen } from '@/custom/screens/podcasts/PodcastsScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function PodcastsPage() {
  return <PodcastsScreen />
}
