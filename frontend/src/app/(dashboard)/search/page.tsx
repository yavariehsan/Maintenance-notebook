import { SearchScreen } from '@/custom/screens/search/SearchScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function SearchPage() {
  return <SearchScreen />
}
