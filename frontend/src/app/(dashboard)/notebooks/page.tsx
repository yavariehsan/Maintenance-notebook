import { NotebookListScreen } from '@/custom/screens/notebooks/NotebookListScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function NotebooksPage() {
  return <NotebookListScreen />
}
