import { SourceListScreen } from '@/custom/screens/sources/SourceListScreen'

// Routing boundary stays here; screen composition lives downstream.
export default function SourcesPage() {
  return <SourceListScreen />
}
